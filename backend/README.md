# backend
Backend Spring Boot 3.2.x (Java 21 LTS). Codigo fuente en src/ (estructura Maven estandar en raiz). Este directorio se mantiene por compatibilidad con la estructura minima del Listing 1 (p.17) — ver src/main/java/.
